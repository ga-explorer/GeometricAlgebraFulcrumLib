namespace GraphicsComposerLib
{
    //TODO: Complete this
    public sealed class AnimatedGifComposer
    {
        //public void Save(IEnumerable<Bitmap> imagesList, int animationDelay)
        //{
        //    using (var collection = new MagickImageCollection())
        //    {
        //        foreach (var image in imagesList)
        //        {
        //            collection.Add(new MagickImage(image));
        //            collection[0].AnimationDelay = animationDelay;
        //        }
                
        //        //// Optionally reduce colors
        //        //QuantizeSettings settings = new QuantizeSettings();
        //        //settings.Colors = 256;
        //        //collection.Quantize(settings);

        //        // Optionally optimize the images (images should have the same size).
        //        collection.Optimize();

        //        // Save gif
        //        collection.Write("Snakeware.Animated.gif");
        //    }
        //}
    }
}
